# Kjerneavhengigheter for Nordlys (runtime)
pandas>=1.5
PySide6>=6.5
requests>=2.31
requests-cache>=1.1

# Rapportering og eksport
openpyxl>=3.1
xlsxwriter>=3.0
reportlab>=3.6

# Kvalitetssikring og validering
# Valgfritt: aktiver XSD-validering ved å installere xmlschema separat
xmlschema>=2.2 ; extra == "xsd"
